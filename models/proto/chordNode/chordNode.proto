syntax = "proto3";

option go_package = "./chordNodeModel";

service Chord {
    rpc Ping(Request) returns (Response);
}

message Request{

}

message Response {
    string Message = 1;
}

message Node {
    string Id = 1;
}
